You are an expert full-stack developer. We are upgrading our minimalist social sports prediction app, 'Huddle', from a static UI mockup into a fully functional MVP on Replit. 

CRITICAL INSTRUCTION: Do not alter the existing layout hierarchies, Tailwind configurations, typography, or the 'Deep Dark Slate' background. Keep the exact monochrome color palette styling shown in the current UI images: strictly clean white text, 1-pixel thin light grey borders, and no background fills for cards. Only edit the Javascript/Typescript logic to connect the UI elements to our backend/database.

Please systematically implement the following functionality across the application:

### 1. Home Feed & Social Posts
* Add Post Functional Form: Add an inline text input form at the top of the Home feed allowing users to create a text-only post. The input must match the current style: clean white text on a container with a 1px grey border and no background fill.
* Submission Logic: Submitting a post must push it directly to the live feed database array with the user's details, a real-time timestamp, 0 likes, and an empty comments array.
* Permanent Record Rule: Ensure text posts and comments have absolutely no "delete" button or option once posted.
* Direct Callouts: Update the text post logic to detect `@username` tags. If a user tags another user and includes a number, register it as a "Pending Callout". The tagged user must see an option on their feed to "Accept" or "Decline". If accepted, lock those points from both balances into a winner-takes-all pot.

### 2. Navigation & Profile Adjustments
* Remove Tab: Delete the "Help and Support" tab completely from the profile screen.
* Privacy Policy Content: Wire the "Privacy Policy" tab to open a basic user agreement modal covering user-generated content and standard platform usage terms.
* Functional Sign Out: Wire the Sign Out button at the top to clear the active session state and redirect the user instantly back to the Sign Up or onboarding screen.

### 3. Invitation Codes
* Code Processing: Implement logic so that when a user inputs a group or leaderboard invite code, their user ID is immediately appended to that specific leaderboard's user array, rendering them on that group's leaderboard screen.

### 4. Game Economy & Gamification
* The Starting Bankroll: Initialize every user's starting balance at exactly 10,000 points when they join a leaderboard cycle.
* The Daily Drip: Give users a +100 point daily login bonus every 24 hours to keep them checking the app without destroying the high-stakes economy.
* The Bankruptcy Penalty (Crimson Red): If a user hits 0 points, lock them from making normal wagers until they claim a +100 point emergency bailout. While their points remain under a 500-point recovery threshold, display a sharp, distinct "BANKRUPT" status tag in bright crimson red typography directly on their minimalist profile card to serve as a public social penalty. This is the ONLY non-monochrome color allowed in the UI.
* The 'All-In' Button: Add a clear "ALL-IN" text button next to the prediction wager input field. Clicking it must immediately populate the input box with the user's entire remaining point balance. Style it cleanly using the standard white or muted grey text to match the rest of the form.
* The 'Heater' Streak: Track consecutive correct predictions via a `currentStreak` variable. If it hits 3 or higher, apply a crisp, bright white 1px border highlight to their profile or username across the leaderboards and feeds. Reset to 0 immediately on a loss.
* Performance Titles: Automatically calculate the user's `winRate` percentage and display a title in small, muted grey text under their points:
    * Under 30% win rate: "Benchwarmer"
    * 30% to 65% win rate: "Coin Flipper"
    * Above 65% win rate: "The Oracle"