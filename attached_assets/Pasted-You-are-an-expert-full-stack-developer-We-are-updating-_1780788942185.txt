You are an expert full-stack developer. We are updating our minimalist social sports prediction app called 'Huddle' built on Replit. I need you to implement the following structural updates, logic enhancements, and economy mechanics into our current codebase. 

Maintain our exact visual style: a premium, ultra-modern 'Deep Dark Slate' (almost black) background, thin 1-pixel borders, elegant fintech-inspired typography, and minimalist UI components. No large, aggressive elements.

Please systematically implement the following 5 requirements:

---

### 1. Home Page: Add Post Functional Form
* Add an inline or clean minimalist modal/form at the top of the Home feed allowing users to create a text-only post.
* The input field should feature clean white text on a clear or 1px grey-bordered container (no background fill).
* When a user submits a post, it must push to the live scrolling social timeline array with their username, a small minimalist placeholder avatar, a real-time timestamp, 0 likes, and an empty comments array.
* Ensure text posts do not have a delete option once posted, adhering to our "permanent record" philosophy.

---

### 2. Profile Page & Navigation Adjustments
* **Remove Tabs:** Delete the "Help and Support" tab entirely from the profile page.
* **Privacy Policy Content:** Convert the "Privacy Policy" tab into an interactive modal or view containing a standard, basic social platform User Agreement (e.g., terms covering user-generated content, assumption of prediction risk, and standard privacy protections).
* **Sign Out Button Logic:** Wire up the existing Sign Out button at the top of the app. Clicking it must clear the current session/user state and redirect the user immediately back to the initial Sign Up / onboarding screen.

---

### 3. Invitation / Group Codes Functional Logic
* Ensure that group or leaderboard invite codes entered by a user are fully processed.
* When a valid code is submitted, append the current user's record to that specific leaderboard/group's user array so they show up on that group's leaderboard screen.

---

### 4. Game Economy: The Bankroll, Daily Drip, and Bankruptcy
Implement the following point-tracking logic for every user account profile:

* **The Starting Bankroll:** When a user initializes or joins a new leaderboard cycle, default their profile's current points balance to exactly 10,000 points (their core war chest).
* **The Daily Drip:** Create a background check or a "Claim Daily Bonus" function. Once every 24 hours upon logging in or opening the app, add a flat +100 points to their total balance. This is a small drip to keep them engaged without resetting their overall standing.
* **The Bankruptcy Penalty & Status:** * If a user's total points balance hits exactly 0, lock them out from placing standard high-value wagers. 
    * They must click a "Claim Daily Bailout" button to get a tiny emergency allowance (+100 points) to stay in the game.
    * **Crucial UI State:** As long as a user is surviving on the Bailout (meaning they hit 0 and haven't naturally earned their way back past a sustainable 500-point threshold), display a prominent, text-based, minimalist **"BANKRUPT"** status tag directly on their profile stats card (right alongside their point totals, previous wagers, and win rate). The badge should only clear once their balance crosses back over 500 points.

---

### 5. Review and Refactor
* Review your data structures to ensure the user object tracks: `points` (integer), `isBankrupt` (boolean), `winRate` (percentage), `previousWagers` (array/count), and active `joinedGroups` (array of strings).
* Ensure all updated state elements smoothly update the UI without throwing breaking errors or losing the existing dark-mode layout scheme.
