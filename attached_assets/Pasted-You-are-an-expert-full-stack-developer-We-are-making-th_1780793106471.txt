You are an expert full-stack developer. We are making three surgical additions to our 'Huddle' MVP. 

CRITICAL INSTRUCTION: DO NOT alter or rewrite our existing database schema, routing, core game economy (bankroll/drip), or basic feed posting logic. We are only adding the specific features listed below. Maintain our strict monochrome black and white UI (with the exception of our existing red 'BANKRUPT' tag). 

Please implement ONLY the following 3 features:

### 1. Update Performance Title Styling
* Locate the existing logic rendering the user performance titles ("Benchwarmer", "Coin Flipper", "The Oracle").
* Update the UI styling for these titles so they stand out more against the deep dark slate background. 
* Increase the font size slightly above standard body text and enclose the text inside a subtle, muted dark grey background container with a 1px light grey border (similar to the visual style of a points tab or pill badge). 

### 2. Group Sentiment & Contrarian Bonus (Match Cards)
* On the individual match prediction cards, calculate and display what percentage of the user base (or specific group) voted for Team A versus Team B.
* Render this visually as a thin, minimalist light grey progress/split bar directly below the match question.
* Add backend logic: If a user places a wager on the minority outcome (under 30% of the total vote) and that prediction wins, automatically apply a 2x multiplier to their final point payout.

### 3. The "Receipt" Share Modal
* Add a native "Share" icon (a simple white outline SVG) to completed/resolved match cards in the feed.
* When clicked, do NOT use an image-generation library. Instead, open a full-screen, heavily stylized DOM modal overlay. 
* This modal must isolate and clearly display the original match question, the final result, and the specific user's 'FAILED' or 'WON' prediction alongside their username.
* Design the layout of this modal specifically so the user can easily take a clean, native mobile screenshot of it to share on other social platforms. Include a functional "Close" button to dismiss the modal.